# Python — Fondamentaux

Bases indispensables avant d’utiliser le stack HFT (`numpy`, `pyzmq`, `asyncio`, etc.).

| Fichier | Sujet |
|---------|--------|
| `01_types_and_variables.py` | Types, opérateurs, immutabilité |
| `02_control_flow.py` | if / for / while / compréhensions |
| `03_functions_and_modules.py` | Fonctions, `*args`, imports |
| `04_data_structures.py` | list, dict, set, deque |
| `05_classes_and_oop.py` | Classes, dataclasses, héritage |
| `06_exceptions_and_context.py` | try/except, `with`, gestion d’erreurs marché |
| `07_file_io_and_json.py` | Fichiers, JSON (configs stratégie) |
| `08_numpy_intro.py` | Premiers pas NumPy (vecteurs de prix) |

```bash
python 01_types_and_variables.py
# … ou tous :
for f in 0*.py; do python "$f"; done
```
